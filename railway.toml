# Railway deploy config for Scraper-WNBA.
# Ensures the correct start command is used.

[deploy]
startCommand = "node jobs/runPlayerWorkers.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
